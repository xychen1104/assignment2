# Notebook note

The main runnable solution is in `src/run_pyarrow_pipeline.py` because the raw parquet files are very large. A notebook can be added for small-sample exploration, but running the full dataset in a notebook is not recommended.
